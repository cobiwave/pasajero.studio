import type { StoryFn } from '@storybook/react'
import type { ViewProps } from './{{titleCase name}}.view'

import { Svgs } from '#/svg-imports'

import { View } from './{{titleCase name}}.view'

export default { title: 'components/{{titleCase name}}' }

export const Default: StoryFn<ViewProps> = (args) => {
  return <View {...args} />
}

Default.args = {
  label: 'Click me'
}

Default.argTypes = {
  icon: { options: [undefined, ...Object.keys(Svgs)], control: { type: 'select' } }
}
